export const intialState = {
    basket: [],
};

function reducer(state, action) {
    switch(action.tyoe) {
        case 'ADD_TO_BASKET':
            break;
            case ''
    }
}

export default reducer;